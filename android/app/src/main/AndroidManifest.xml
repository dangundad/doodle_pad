<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 광고 ID 권한 -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <!-- 네트워크 권한 (광고, 인앱 구매) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 인앱 결제 권한 -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- 진동 피드백 -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!--
      갤러리 저장(gal 플러그인) — API 29 이하에서는 MediaStore가 아닌 legacy
      external storage 경로로 쓰기 때문에 이 권한이 반드시 필요하다.
      minSdk가 24라 이 선언이 없으면 Android 7~10 사용자는 "저장 권한이 필요합니다"
      토스트만 보고 갤러리 저장을 영영 성공시킬 수 없다(권한 요청 자체가 즉시 거부됨).
      API 30+ 에서는 선언이 무시된다.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application android:label="@string/app_name" android:name="${applicationName}" android:icon="@mipmap/launcher_icon" android:roundIcon="@mipmap/launcher_icon" android:requestLegacyExternalStorage="true">

        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:taskAffinity="" android:theme="@style/LaunchTheme" android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data android:name="flutterEmbedding" android:value="2" />

        <!-- AdMob App ID -->
        <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="ca-app-pub-9645460570589541~8667399406"/>
        <meta-data android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION" android:value="true"/>
        <meta-data android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING" android:value="true"/>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
